<!DOCTYPE html>
<html>
<head>	
		<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
		<link rel='stylesheet' href='/css/calendar.css' type='text/css'  />
		<script type="text/javascript" src="http://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>		
</head>
<body onload="time()" >
	<div id="homnay" >
		<h4>[Practice nodejs, html, css, jquery basic. 
		Nguyễn Hoài Danh - 06/2018]</h4>
		<h2 id="timeToday"></h1>
		<h2 id="LunartimeToday"></h1>
		<h2>
			<i id="clock" class="fa fa-clock-o" style="font-size:48px;">
			</i></h2>	
	</div>
	
	
	<div class="table" align="center" >
			<input type="date" id="myDate" >
			<button id="done" >Xem lịch</button>
					
	</div>		
		
			
</body>
		<script type="text/javascript" src="/js/calendar.js"></script>
				
		<script>
		var now=new Date();
		var temp1=now.toString().split(" ");
		//ngay thang nam hien tai
		var Day=temp1[2];
		var Month=mnths[temp1[1]];
		var Year=temp1[3];
	
		//set input time = ngay hien tai
		$('#myDate').val(convert(new Date().toString()));	
		var mnths = { 
						Jan:"01", Feb:"02", Mar:"03", Apr:"04", May:"05", Jun:"06",
						Jul:"07", Aug:"08", Sep:"09", Oct:"10", Nov:"11", Dec:"12"
		}
		
		if('<%=data.month%>'!=''&&'<%=data.year%>'!='')
		{	
			createTable('<%=data.month%>','<%=data.year%>');
			$('#tieude').html('LỊCH ÂM & DƯƠNG <font color="red">'+'<%=data.month%>'+'/'+'<%=data.year%>'+'</font>');
		}
		else {	// in lich hien tai
			createTable(Month,Year);
			$('#tieude').html('LỊCH ÂM & DƯƠNG <font color="red">'+Month+'/'+Year+'</font>');
		}
		
		
		//set width=width
		var w=$('#calendar').css('width');
		$('#homnay').css('width',w);
		
		var tem,tem1,url;
		$('#next').click(function ()
		{	if('<%=data.month%>'!='')
			{
			 tem=parseInt('<%=data.month%>')+1;
			 tem1='<%=data.year%>';
			}
			else {
			 tem= parseInt(Month)+1;
			 tem1=Year;
			}
			if(tem==13){tem=1;
				tem1++;
			}
			 url='/'+tem+'/'+tem1;
			window.location.href=url;
		});
		
		$('#back').click(function ()
		{	if('<%=data.month%>'!='')
			{
			 tem=parseInt('<%=data.month%>')-1;
			 tem1='<%=data.year%>';
			}
			else {
			 tem= parseInt(Month)-1;
			 tem1=Year;
			}
			if(tem==0){
				tem=12;
				tem1--;
			}
			 url='/'+tem+'/'+tem1;
			window.location.href=url;
		});
		</script>
</html>